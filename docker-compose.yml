version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: krpoprodaja-redis
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    command: >
      sh -c "
      if [ -n \"$REDIS_PASSWORD\" ]; then
        redis-server --requirepass $REDIS_PASSWORD --appendonly yes
      else
        redis-server --appendonly yes
      fi
      "
    environment:
      - REDIS_PASSWORD=${REDIS_PASSWORD:-}
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - krpoprodaja-network

  # Optional: PostgreSQL database
  # Uncomment if you want to run PostgreSQL in Docker too
  # postgres:
  #   image: postgres:16-alpine
  #   container_name: krpoprodaja-postgres
  #   ports:
  #     - "5432:5432"
  #   environment:
  #     POSTGRES_USER: ${POSTGRES_USER:-krpoprodaja}
  #     POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password}
  #     POSTGRES_DB: ${POSTGRES_DB:-krpoprodaja}
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-krpoprodaja}"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #   restart: unless-stopped
  #   networks:
  #     - krpoprodaja-network

volumes:
  redis_data:
    driver: local
  # postgres_data:
  #   driver: local

networks:
  krpoprodaja-network:
    driver: bridge
